# Packages

[English](README.md) | 简体中文

本目录包含 DSH 原生插件组与可复用的运行时库。

- [`dsh-formal-proof`](dsh-formal-proof/README.zh-CN.md)：一个可安装的 DSH Bundle，由 Observer、Role、Reflection、Runtime 和控制工具插件组成。

领域契约与 Verifier 逻辑可以在不启动 DSH 的情况下独立导入；DSH 适配层只使用公开的 Service、Tool、Prompt、Event 与 Agent 创建接口。
